---
description: Build the EstateIQ client for production
---

## Steps

// turbo-all

1. Build the production bundle from the project root:
```
npm run build
```

This builds the Vite/React client into `client/dist/`. The output is optimized and ready for deployment.
